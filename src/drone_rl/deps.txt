pip install stable-baselines3==2.0.1  # includes PPO, SAC, TD3, etc.
pip install gym==0.26.3               # SB3 compatible Gym version
pip install shimmy                     # required by SB3 VecEnv wrappers
pip install torch>=2.0.1,<3          # PyTorch, GPU or CPU
pip install numpy>=1.23
